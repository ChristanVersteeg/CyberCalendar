name: Daily Tip Update

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  update-tip:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Get current tip
        id: tip
        env:
          TIPS_JSON: ${{ secrets.TIPS_JSON }}
        run: |
          DAY=$(date +%j | sed 's/^0*//')
          TIP=$(echo "$TIPS_JSON" | jq -r ".tips[$((DAY-1))].text")
          echo "tip=$TIP" >> $GITHUB_OUTPUT

      - name: Get current image (PNG only, with auth logging)
        id: image
        env:
          TIPS_JSON: ${{ secrets.TIPS_JSON }}
          GITHUB_TOKEN: ${{ secrets.PAT }}
        run: |
          set -euo pipefail
          set -x
      
          # 1) Which day-of-year are we on?
          DAY=$(date +%j | sed 's/^0*//')
          echo "🔢 DAY: $DAY"
      
          # 2) Pull the bare image name from your JSON
          IMAGE_BASE=$(jq -r ".tips[$((DAY-1))].image" <<< "$TIPS_JSON")
          echo "🖼 Raw name: '$IMAGE_BASE'"
      
          # 3) Bail out early if no image specified
          if [[ -z "$IMAGE_BASE" || "$IMAGE_BASE" == "null" ]]; then
            echo "⚠️  No image for today"
            echo "image=" >> $GITHUB_OUTPUT
            exit 0
          fi
      
          # 4) Build URL for PNG
          IMG="${IMAGE_BASE}.png"
          URL="https://api.github.com/repos/ChristanVersteeg/CyberTips/contents/images/${IMG}"
          echo "🌐 Attempting to download $IMG from:"
          echo "   $URL"
      
          # 5) Fetch with headers dump so we can inspect auth scopes
          STATUS=$(curl -sSL \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github.v3.raw" \
            -D headers.txt \
            -w "%{http_code}" \
            -o "$IMG" \
            "$URL" || true)
      
          echo "📬 HTTP status: $STATUS"
          echo "📑 Response headers (excerpt):"
          head -n 10 headers.txt | sed 's/^/   /'
      
          # 6) Check for auth errors
          if [[ "$STATUS" == "401" ]]; then
            echo "❌ Authentication failed (401 Unauthorized)."
            echo "   • Check that GITHUB_TOKEN/PAT is set correctly."
            echo "   • Verify it has at least repo:read permission on ChristanVersteeg/CyberTips."
            rm -f "$IMG"
            IMAGE=""
          elif [[ "$STATUS" == "403" ]]; then
            echo "❌ Access forbidden (403)."
            echo "   • The token may lack sufficient scopes or be rate‑limited."
            echo "   • Inspect 'X-OAuth-Scopes' in headers above."
            rm -f "$IMG"
            IMAGE=""
          elif [[ "$STATUS" == "200" && -s "$IMG" ]]; then
            echo "✅ Successfully fetched $IMG ($(stat -c '%s' "$IMG") bytes)"
            IMAGE="$IMG"
          else
            echo "⚠️  Failed to fetch $IMG (status $STATUS)."
            rm -f "$IMG"
            IMAGE=""
          fi
      
          # 7) Final workspace check
          echo "📂 Workspace contents:"
          ls -lha .
      
          # 8) Export the result
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
      
        
        
        
        
        
      - name: Prepare gh-pages branch
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git checkout --orphan temp
          git rm -rf .

      - name: Create index.html (with debug)
        env:
          IMAGE: ${{ steps.image.outputs.image }}
        run: |
          set -euxo pipefail
      
          echo "🔢 IMAGE env var is: '$IMAGE'"
      
          # Start building the page
          cat <<EOF > index.html
          <!DOCTYPE html>
          <html>
            <head>
              <meta charset="UTF-8">
              <title>Daily Tip</title>
            </head>
            <body>
              <h1>Today's Tip:</h1>
              <div id="tip"></div>
          EOF
      
          # Log and inject the <img> if present
          if [ -n "$IMAGE" ]; then
            echo "✅ Adding <img> tag for '$IMAGE'"
            cat <<EOF >> index.html
              <div id="tip-image">
                <img src="$IMAGE" alt="Daily tip illustration" />
              </div>
            EOF
          else
            echo "⚠️  IMAGE is empty—skipping <img> tag"
          fi
      
          # Finish up
          cat <<EOF >> index.html
              <script>
                fetch('tip.txt')
                  .then(r => r.text())
                  .then(t => document.getElementById('tip').textContent = t);
              </script>
            </body>
          </html>
          EOF
      
          # Finally, dump the generated HTML into the logs for inspection
          echo "🔍 Generated index.html content:"
          sed -n '1,200p' index.html
        
        

      - name: Add daily tip
        run: |
          echo "${{ steps.tip.outputs.tip }}" > tip.txt
          git add tip.txt index.html
          git commit -m "Daily tip update: $(date +%F)"

      - name: Force push to gh-pages
        run: |
          git branch -D gh-pages || true
          git branch -m gh-pages
          git push -f origin gh-pages